/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "avatars.githubusercontent.com",
      "loremflickr.com",
      "cloudflare-ipfs.com",
    ],
  },
};

export default nextConfig;
